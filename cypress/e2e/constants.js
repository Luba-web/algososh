const circle = 'li';

const colorModified = '[class*=circle_modified]';
const colorChanging = '[class*=circle_changing]';
const colorDefault = '[class*=circle_default]';

export default { circle, colorModified, colorChanging, colorDefault };
